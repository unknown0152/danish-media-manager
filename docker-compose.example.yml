services:
  danish-media-manager:
    image: ghcr.io/unknown0152/danish-media-manager:v0.34.0
    container_name: danish-media-manager
    environment:
      PROWLARR_URL: http://prowlarr:9696
      PROWLARR_API_KEY: ${PROWLARR_API_KEY:?set PROWLARR_API_KEY}
      DANISH_INTELLIGENCE_URL: http://danish-intelligence:9699
      RADARR_URL: http://radarr:7878
      RADARR_API_KEY: ${RADARR_API_KEY:-}
      SONARR_URL: http://sonarr:8989
      SONARR_API_KEY: ${SONARR_API_KEY:-}
      SEERR_URL: http://seerr:5055
      SEERR_API_KEY: ${SEERR_API_KEY:-}
      SEERR_SYNC_ENABLED: "true"
      SEERR_SYNC_INTERVAL_SECONDS: "60"
      SEERR_AUTO_GRAB: "true"
      WANTED_SEARCH_ENABLED: "true"
      WANTED_SEARCH_MAX_PER_CYCLE: "10"
      RECENT_FEED_SYNC_ENABLED: "true"
      RECENT_FEED_LIMIT: "500"
      MONITORED_REQUESTS_MAX_PER_CYCLE: "100"
      ALTMOUNT_URL: http://danish-intelligence:9699/altmount
      ALTMOUNT_API_KEY: ${ALTMOUNT_API_KEY:?set ALTMOUNT_API_KEY}
      ALTMOUNT_IMPORT_DIR: /mnt/altmount-import
      ALTMOUNT_MOUNT_PATH: /mnt/altmount
      MEDIA_ROOT: /media
      DATABASE_PATH: /data/danish-media-manager.db
      ALLOW_DIRECT_DOWNLOAD_URLS: "false"
      TMDB_API_KEY: ""
    volumes:
      - ./data:/data
      - /mnt:/mnt:ro,rshared
      - /media:/media:ro
    networks:
      - media-stack
    ports:
      - "8088:8080"


networks:
  media-stack:
    external: true
